








8.CRC_TAB 上注需要指明地址字段含义， CRC_TAB内容大端发送。 最好有示例报文。和升级流程。

10.上注CRC_TAB返回内容与协议描述不一致。

12. 上注CRC步骤建议取消,CRC_TAB固定在程序内部。


9.程序复制时间约9秒  协议里需要体现。实际返回多了一字节。
7.需要补充升级流程图,包括所有的步骤中涉及的参数的明确含义需要说明。
5.填充字节0xFF 0xFF是0x40 0xF6(上一帧的最后两个字节内容)，协议中必须明确。
4.偏移地址是从0开始 协议需要明确.  
3.BOOT和应用中CANID必须保持一致.   交付的三台在boot下CANID均为0x0C
2.读程序指令返回内容、长度域不对;最后6字节有0x7F按照协议应该全部是0xFF.
6.协议中的数据是小端模式(最开始两字节长度是大端) 文档中需要明确，  天仪的协议中全部是大端的。
1.boot下星时同步指令也会有返回
11.CAN ID的最后8位用于区分不同包,不是始终是0.  应用程序中能处理后8位不是0的CANID。boot中进入boot指令发送时,CANID低8位必须是0,boot中与协议不符。